parameters:
  - name: message
    type: string
variables:
    name: message
    value: "World"
steps: 
  - script: echo " ${{ parameters.message }} variable name is ${{ variables.message }} and variable value is ${{ variables.value }}"
    displayName: 'Print message'